# Base URL for API calls. Default "/api" goes through the Vite dev proxy
# (see vite.config.js). For production, set this to your backend URL, e.g.
# VITE_API_URL=https://api.alldrive.com/api
VITE_API_URL=/api
